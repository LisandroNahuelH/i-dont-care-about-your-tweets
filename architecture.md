# Architecture

The architecture guide lives in [docs/architecture.md](docs/architecture.md). This file stays as a pointer so agents and tools that look for `architecture.md` at the repository root find it in one hop.
